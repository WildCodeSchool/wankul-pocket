import Image from "next/image";

export const TradeAcceptButton = () => {
  return (
    <button type="submit">
      <Image src={""} alt="Accepter l'échange" height={50} width={50} />
    </button>
  );
};
